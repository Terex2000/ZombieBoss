#ifndef SHOP_H
#define SHOP_H

#include "Player.h"
#include "Shop.h"
#include "ShopView.h"

class Shop {
public:
    explicit Shop(Player& player);

    bool buyLife(HUDController& hudController, ShopView& shopView); // Acheter une vie
    bool upgradeDamage(HUDController& hudController, ShopView& shopView); // Améliorer les dommages

private:
    Player& player; // Référence au joueur pour modifier ses statistiques

};

#endif // SHOP_H
